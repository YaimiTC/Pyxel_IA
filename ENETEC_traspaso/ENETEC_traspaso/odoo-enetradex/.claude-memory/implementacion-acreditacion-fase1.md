---
name: implementacion-acreditacion-fase1
description: "Implementación funcional del wizard de acreditación + contraparte + ofertas + gate (ODIN 2.0), construida y probada end-to-end"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4c1918a5-33b6-4251-837d-dc0bfe097d44
---

Construido y PROBADO end-to-end en localhost (2026-06-14) el proceso de acreditación/contraparte de [[proyecto-enetec-odin]], según el diseño de [[requisitos-enetec-cliente]]. Plan en `C:\odoo_enetradex\docs\plan_implementacion_acreditacion.md`.

**Módulos tocados:**
- `pyxel_enetradex_backend` (modelos/vistas/datos): chip `crm.lead.en_party_role` (related a `contact_type_id.type_of_contact`) + filtros/group-by en CRM; modelos NUEVOS `en.counterparty.relation`, `en.accreditation.invitation`, `en.supply.offer`(+`.line`), `en.tender`(+`.line`); flags `res.partner.en_is_public_provider` / `en_accepts_offers`; `crm.lead.en_initiated_by`/`en_inviter_partner_id`; GATE en `importation.process` (estado nuevo `en_stage_pending_accreditation` "PENDIENTE DE ACREDITACIÓN" seq 0 + constraint que bloquea avanzar a operables sin `en_both_accredited`); renombrada `importation_stage_solicitud` a "SOLICITUDES PARA ATENDER". security `ir.model.access.csv`. Menú raíz "ENETEC" con vistas de los 4 modelos.
- `pyxel_enetradex_website` (controlador + plantillas): `controllers/wizard.py` con rutas `/en/wizard` (page, auth user), `/en/wizard/submit` (json, crea lead+relación+oferta+tender+proceso+invitación según payload), `/en/catalog/offers` (json public, ofertas publicadas de proveedores públicos filtrables), `/en/accredit/<token>` + `/submit` (página del invitado: auto-acreditación mínima → crea partner+lead `en_initiated_by=counterparty` → notifica al invitador por chatter → invitación `accepted`). Plantillas `en_wizard.xml` (wizard reutiliza el prototipo: define vars CSS sobre `.enwz`; cliente Rol→Datos→Solicitud→Proveedor[ya tengo/buscar catálogo real/ENETEC cotiza]→Resumen; proveedor Rol→Datos→Oferta(cotización)→Clientes→Resumen) y `en_invited.xml`.

**Probado OK:** wizard cliente logueado crea Lead+chip+relación+operación(gate); catálogo filtra por producto (Gasolina→3 ofertas reales); gate: avanzar bloqueado sin acreditar → abogado acredita (lead a etapa `is_accreditation_stage`, que es "Won") → avanza a "SOLICITUDES PARA ATENDER"; invitado crea partner+lead y notifica al invitador; proveedor crea oferta con líneas y total (flete+seguro) y relaciones de cliente.

**Datos sembrados (commit en BD):** 5 productos combustible; 5 proveedores públicos acreditados (PetroWorld, Global Fuels, Energía Atlántico, Caribe Petroleum, MarOil) con ofertas publicadas. Usuarios prueba: cliente.final@enetradex.test (cliente) y felipito@gmail.com (proveedor), clave `Enetec2026`.

**PULIDO (2026-06-14):** documentos del wizard ahora se SUBEN de verdad (inputs file → `S.docFiles` → submit por multipart `FormData` → `/en/wizard/submit` es type=http csrf=False → guarda `ir.attachment` en el partner); "difundir oferta" persiste (crea clientes con `en_accepts_offers=True` + relaciones, `created.diffused`); el wizard ya no usa iconos Tabler (texto); etapa CRM dedicada **"Acreditado"** (seq 70) con `is_accreditation_stage` (movido desde "Won", 6 leads migrados). Todo verificado: subida de doc (adjunto PDF en partner), difusión (2 clientes opt-in), submit FormData en navegador (Lead+Oferta), gate sigue OK.
**UI/entrada (2026-06-14):** TODOS los botones "Acreditarme" e "Importar" del sitio apuntan ahora a `/en/wizard` (no al form viejo `/business-register`): cambiados en el tema (navbar/home/shop/footer/about + JS importations_template/accreditation.js) y en el portal `/my` (`pyxel_import_website/views/portal_template_view.xml` onboard pages + business_registration.xml enlace interno). Esto resolvió el "Oops" que salía al entrar a acreditarme (venía del form viejo). El redirect login/signup ahora es limpio (`/web/login?redirect=/en/wizard`, signup lo conserva) → tras crear cuenta o entrar aterriza en el wizard, no en "Mi cuenta". En el wizard: card proveedor dice "Empresa extranjera"; paso "Mis datos" usa "Nombre de la empresa" + nuevo campo "Objeto social" (manual), ambos se persisten en el `res.partner` (payload.company → controller).

**Textos/UX wizard (2026-06-14):** tarjeta del pliego dice "Que me coticen" (sin "ENETEC"); vista Resumen sin jerga de CRM: muestra Empresa, Objeto social, Documentos (nº subidos), Solicitud (producto·cantidad), Proveedor; etiqueta "Proceso de acreditación" (no "Recorrido en el CRM"); botón "Enviar" (no "Enviar al CRM"); mensaje de éxito "Solicitud enviada para acreditación…". Cantidad de la solicitud se guarda en `S.solQty`. Al pulsar "Enviar" el JS REDIRIGE a una página de confirmación `/en/wizard/enviado` (template `en_wizard_done`): "✓ Solicitud enviada para acreditación" + pipeline + botones "Ir a mi cuenta" (/my) y "Volver al inicio" (/). Así no se queda en la misma vista.

**ETAPAS CRM del negocio (2026-06-15):** el pipeline de acreditación usa las etapas diseñadas (estaban en `pyxel_import_backend/data/crm_stage_data.xml` pero COMENTADO en el manifest y además BLOQUEADO por noupdate del core). Aplicadas por ORM renombrando las 4 etapas Odoo: **CLIENTE O PROVEEDOR POTENCIAL → EN PROCESO DE APROBACIÓN → APROBADOS EN CARTERA → GANADO**. La etapa de ACREDITACIÓN (`is_accreditation_stage`) es **"APROBADOS EN CARTERA"** (crm.stage_lead3, seq 3): al mover un lead ahí, el partner queda `is_accredited` (verificado: dispara el recompute por `@depends opportunity_ids.stage_id`). Se eliminó la etapa extra "Acreditado". OJO con el bug `res.partner.is_accredited/is_accredited2` (compute compartido stored+no-stored): cambiar SOLO el flag de etapa no recalcula los partners → hubo que fijar `is_accredited` por SQL una vez. Los leads de acreditación se crean SIN comercial (`user_id=False`); menú **ENETEC → "Solicitudes de acreditación"** muestra todos sin filtro "Mi pipeline".

**FORM DEL LEAD pulido (2026-06-15):** pestaña "Acreditación" en `crm.crm_lead_view_form` (inherit en pyxel_enetradex_backend/views/crm_lead_views.xml) con datos del partner via related (en_partner_ctype/mtype/vat/objeto/accredited) + `en_document_ids` (Many2many ir.attachment computed, res_model=crm.lead) mostrado con `widget="many2many_binary"` (tarjetas descargables). Los documentos del wizard se adjuntan ahora al LEAD y al partner (controller). Agrimpex NO tenía form pulido en código (crm_lead_views.xml idéntico, 31 líneas); el pulido era a nivel de BD desplegada.

**Menor restante:** relación se marca 'active' si el proveedor está acreditado (podría afinarse a "ambos"); "buscar en cartera" del cliente usa nombres de muestra (crea stub) — el path real es el catálogo público.

**PROCESO IMPORTACIÓN — datos y gate visibles (2026-06-15):** en `importation.process` (inherit en pyxel_enetradex_backend) se añadieron `en_customer_accredited`/`en_provider_accredited` (related a is_accredited) y `en_accreditation_status` (computed: "Falta por acreditar: cliente «X» y proveedor «Y»" o "Ambas partes acreditadas"). El kanban heredado (`pyxel_import_backend.view_importation_process_kanban`) ahora muestra **Cliente:** (antes solo salía provider) + **Proveedor:** + badge de estado (widget="badge" con decoration-success/danger; OJO: decoration exige `<field name="en_both_accredited" invisible="1"/>` presente en la tarjeta — `record.X.raw_value` en t-attf NO carga el campo y rompe el kanban). El form heredado tiene alerta arriba (lock/ check) + toggles por parte. El mensaje del gate (constraint) ahora nombra a quién falta: "No se puede avanzar «IMP00004» a «X»: todavía falta acreditar el cliente «Pyxel»…".

**SEGUIMIENTO punta a punta (2026-06-15):** flujo de seguimiento que ve el cliente en su cuenta, actualizable por comercial (manual) o automático.
- Modelo NUEVO `en.tracking.event` (pyxel_enetradex_backend/models/tracking_event.py): phase (accreditation/importation), event_type (created/stage/doc/note), stage_name, date, user_id, source (auto/manual), lead_id|operation_id, partner_id. Método `en_log_event(record, phase, stage_name, partner, source, event_type, note)` crea el evento y llama `_en_notify`: hace `record.message_post(body, partner_ids=[cliente])` → **portal + correo en un paso** (el email sale si el partner tiene correo; en dev queda encolado en mail.mail). `_en_notify_whatsapp` es STUB (punto de integración Twilio/WhatsApp Business), se activa con config param `en_seguimiento.whatsapp_enabled=True` (hoy False).
- Hooks: `create()`/`write()` de crm.lead (solo si en_party_role) e importation.process registran evento al crear ("Solicitud recibida") y al cambiar stage_id. source='manual' por defecto; pasar context `en_auto_event=True` lo marca 'auto'.
- Menú **ENETEC → Eventos de seguimiento** (tree auditoría). Security: group_user RW, group_portal R.
- Portal (`pyxel_import_website`): controlador `importation_view` calcula `timeline` (eventos del proceso + acreditación del cliente Y del proveedor vía `expression.OR`, porque el gate depende de ambos), `accred_docs` (adjuntos del res.partner), `customer_accredited`/`provider_accredited` y `action_required`/`action_kind`. Plantilla `importation_view` (imports_frontend_views.xml): alerta "Acción requerida" + badges de ambas partes + **línea de tiempo** (puntos verde=auto/azul=comercial, etiqueta "Tu empresa"/"Proveedor" en eventos de acreditación, fecha, responsable) + documentos descargables + **chatter SIEMPRE visible**.

**ESTÉTICA PORTAL (2026-06-15):** la vista de seguimiento se rediseñó con la paleta del sitio (de `agrimpex_redesign.scss`: azul `#0f44ce`, ink `#14233f`, muted `#5b6b85`, línea `#dde5f0`, cards radius 14px sombra `0 2px 14px rgba(0,0,0,.06)`). Bloque `<style>` embebido con prefijo `.en-track` (igual patrón que el wizard `.enwz`). TODO en tarjetas de marca: resumen izq (sticky, labels mayúsculas), banner acción con icono circular, pills ok/wait, timeline `.en-tl` con dots, chips `.en-doc`, y "Datos de la operación" + chatter envueltos en `en-card`. Columnas reequilibradas 3/9.

**WIZARD MODO "SOLO IMPORTACIÓN" + AUTO-AVANCE GATE (2026-06-15):** (1) Auto-avance: cuando un lead pasa a etapa de acreditación (is_accreditation_stage), `crm.lead.write` llama `importation.process._en_advance_for_partners(partners)` que mueve a "SOLICITUDES PARA ATENDER" las operaciones en gate de esos partners SOLO si `en_both_accredited` (la operación común espera a las dos partes). Además `importation.process.create` ahora pone la etapa inicial en SOLICITUDES (no gate) si ambas partes ya están acreditadas al crearse → operar con contrapartes acreditadas no pasa por el gate. (2) Wizard importar: los botones "Importar"/"Nueva importación" usan `?op=1`; `/en/wizard` con ?op=1 para un acreditado ya no redirige a /my/seguimiento sino que renderiza el wizard pasando data-attributes (data-op/data-accredited/data-role/data-company) al `.enwz`. El JS detecta modo importOnly (op+accredited+role cliente) → `S.importOnly=true`, arranca en "Solicitud" con SOLO 3 pasos (Solicitud→Proveedor→Resumen) vía función `steps()` (antes era array P fijo de 5; ahora dinámico). panelRes y la página `/en/wizard/enviado?op=1` (template en_wizard_done con wz_op) muestran textos de importación, no de acreditación. El submit reutiliza el lead existente (no re-acredita) y crea una nueva importation.process. Probado E2E: PYME acreditada → Importar → solicitud Diésel 250000 → proveedor PetroWorld (catálogo) → IMP nueva creada directa en SOLICITUDES PARA ATENDER. BUG arreglado en wizard.py: al crear una 2ª operación con un proveedor con el que ya existía relación, `Rel.create` violaba la constraint única `en_counterparty_relation_uniq_client_supplier` y el try/except NO sirve en Postgres (la transacción queda abortada) → ahora se BUSCA la relación y se reutiliza (varias operaciones comparten una relación). El mensaje de `/en/wizard/enviado?op=1` (template en_wizard_done con wz_op) dice "Solicitud de importación enviada" (no "para acreditación") con pipeline de importación y botón "Ver mis operaciones".

**TARJETA CRM: doble acreditación + diferenciación (2026-06-15):** una solicitud puede generar DOS acreditaciones (la empresa propia + la contraparte que registra vía cphas='si'/cp='info'; se enlazan por `crm.lead.en_inviter_partner_id`). Campos computados nuevos en crm.lead: `en_doc_count` (len en_document_ids), `en_origin_label` ("Acreditación propia" / "Registrado por X"), `en_also_registered` (nombres de leads cuyo en_inviter_partner_id == este partner). Tarjeta del kanban (`en_acc_kard` con clase is-cli/is-sup): chip de tipo con icono+color (Cliente azul fa-building-o / Proveedor ámbar fa-ship) + pill de estado (Acreditado verde/Pendiente gris) + origen + "N documento(s) subido(s)" + "También registró a: X". Estilos `.en_acc_kard` en agrimpex_backend.scss (fuera de .en-track porque la tarjeta se inyecta en el card del crm kanban). En el FORM, en_timeline_html añade card "Vínculo de acreditación" (link al lead de la contraparte + su estado, o "Registrado por X") + nota `.en-note` con la regla: la operación que une a ambas partes solo avanza con las dos acreditadas, pero la empresa acreditada puede iniciar nueva operación con otra contraparte acreditada (?op=1). Sin lógica de bloqueo nueva (el gate ya bloquea); solo texto claro.

**CRM REDISEÑADO COMO VISTA DE IMPORTACIÓN (2026-06-15):** OJO el CRM que usa el cliente es el **Pipeline estándar** (`crm.crm_lead_action_pipeline`, menú CRM), NO el menú ENETEC. (1) KANBAN: heredada `crm.crm_case_kanban_view_leads` añadiendo badges en la tarjeta: Tipo (Cliente=info/Proveedor=warning) y Estado (Acreditado=success/Pendiente=secondary) + NIT. Usar el patrón nativo del crm_kanban: declarar `<field>` antes de `<templates>` y leer `record.X.raw_value` (label_selection NO rinde en kanban; el campo nuevo `crm.lead.en_accred_state` selection computed da el badge de estado). (2) FORM: panel tipo importación arriba (en_timeline_html ahora incluye banner + **pipeline de etapas** de crm.stage resaltando la actual + tarjeta "Datos de la empresa" Tipo/NIT/Naturaleza/Tipo de contacto/Objeto social + recorrido). Se OCULTÓ el ruido de ventas: el `<h2 class="row g-0">` de Ingresos esperados/Probabilidad y el `div[@name='button_box']` con `<attribute name="class" add="d-none" separator=" "/>` (OJO: `invisible="1"` NO funciona en un `<h2>` plano → usar clase d-none vía CSS); date_deadline y tag_ids sí se ocultan con invisible. Estilos `.en-kv` y `.en-pipeline` en agrimpex_backend.scss (backend).

**ESTÉTICA CRM = PORTAL (2026-06-15):** el form del lead (crm.lead) ahora muestra el MISMO concepto visual del portal de importación. Campo computado `crm.lead.en_timeline_html` (Html, sanitize=False, Markup) construye un banner acreditado(verde)/pendiente(ámbar) + tarjeta "Recorrido de acreditación" con línea de tiempo de `en_tracking_ids` (One2many a en.tracking.event por lead_id), usando las clases `.en-track` ya definidas. Esas clases se añadieron al backend en `agrimpex_backend.scss` (web.assets_backend) con la paleta ENETRADEX (azul $en-blue, verde #15803d, ámbar #e08a00). Colocado en crm_lead_views.xml con `<field name="en_timeline_html" nolabel="1" readonly="1"/>` antes del notebook (sobre los documentos). OJO: ordenar eventos por `(date, id)` (no solo date) porque dos writes en el mismo segundo quedan ambiguos; fecha mostrada con `fields.Datetime.context_timestamp`. El @depends NO puede usar `en_tracking_ids.stage_id` (el modelo de eventos tiene `stage_name`, no stage_id). Verificado: lead acreditado=verde+timeline, lead pendiente=ámbar.

**MI CUENTA + /my/seguimiento (2026-06-15):** la "Mi Cuenta" del portal se rediseñó con la marca y se le añadió el seguimiento. Estilos `.en-track` CENTRALIZADOS en `pyxel_enetradex_custom_web_theme/static/src/scss/en_track.scss` (web.assets_frontend) — se quitó el `<style>` inline de imports_frontend_views.xml; ese scss incluye también el rediseño de las tarjetas de Mi Cuenta (`#wrap .o_portal_category .card` redondeadas+sombra+hover azul) y el banner `.en-acc-banner` del panel. Página NUEVA `/my/seguimiento` (controlador en portal_controller.py, plantilla portal_my_seguimiento en imports_frontend_views.xml usando t-call portal.portal_layout): banner acreditado/pendiente + recorrido de acreditación (en.tracking.event del partner) + lista de operaciones (`.en-op`) con badge de etapa que enlaza a /importations/view. Mi Cuenta (tema portal_home.xml): tarjeta NUEVA "Mi acreditación"→/my/seguimiento (icono acreditacion.png) y el panel derecho muestra `en-acc-banner` (is_accredited, ahora pasado por _prepare_portal_layout_values) + link "Ver seguimiento completo". **PERMISOS PORTAL (clave):** el detalle /importations/view daba 403 al cliente porque el portal no tenía acceso a importation.stage/importation.process. Añadidos ACL group_portal READ (`importation.process`, `importation.stage`, y `pyxel_import_backend.model_res_partner_contact_type`/`management_type` — OJO: usar el prefijo de módulo completo, el bare `model_res_partner_contact_type` NO resuelve desde pyxel_enetradex_backend) + record rules en `security/portal_rules.xml` (cliente solo ve SUS importation.process por customer_id/provider_id y SUS en.tracking.event por partner_id). Esto además hizo que la tabla OWL `/model/imports` muestre las operaciones del cliente (antes vacía por falta de acceso a res.partner.contact.type).

**DASHBOARD Mi Cuenta (2026-06-15):** `/my` y `/my/home` se overridearon (Portal.home en portal_controller.py) para renderizar `portal_my_dashboard` (server-rendered = datos frescos al abrir; con try/except que cae a super().home si algo falla). Método `_en_dashboard_values()` arma datos según rol (cliente/proveedor por contact_type type_of_contact): KPIs role-aware (cliente: operaciones activas/por acreditar/facturas pendientes+monto/próximo arribo; proveedor: ofertas publicadas/operaciones/clientes en cartera/solicitudes abiertas), "Requiere tu atención" (acreditación incompleta, facturas por vencer/vencidas account.move payment_state, operación en gate), "Mis operaciones" con barra de progreso (idx etapa / nº etapas operables seq>0), "Actividad reciente" (en.tracking.event), "Facturas recientes" (account.move out_invoice posted, badge pagada/pendiente/vencida) y campana con contador = nº de pendientes (calculado al abrir, sin tiempo real). Plantilla `portal_my_dashboard` (t-call portal.portal_layout) en imports_frontend_views.xml; estilos `.en-dash` en en_track.scss. OJO: el sitio NO carga Tabler — usar iconos Font Awesome (`fa fa-*`), no `ti-*`. Verificado como PYME (cliente, IMP00006, etapa 2 de 7) y como felipito (proveedor: 3 ofertas, 4 clientes, estados vacíos OK). El override de home deja sin uso las tarjetas viejas de portal_home.xml/portal_template_view.xml (inofensivo).

**PRUEBA E2E COMPLETA (2026-06-15):** registrada PYME `pyme.sur@enetradex.test`/`Enetec2026` (partner 27) → wizard REAL por endpoint autenticado (curl multipart a `/en/wizard/submit` con PDF) creó lead cliente 13 (+doc), proveedor NUEVO no acreditado "Oriente Oil & Gas" (partner+lead 14), relación 8, proceso IMP00006 en gate. Abogado movió ambos leads POTENCIAL→EN PROCESO→APROBADOS (acreditó los dos); el front se actualizó en vivo en cada paso (timeline + acción + badges). Con ambos acreditados, el gate permitió mover IMP00006 a SOLICITUDES→TRÁMITES EN ORIGEN. **2 FALLOS DE REGLA detectados y corregidos:** (1) el path cliente `cpHas='si'/cp='info'` creaba el partner del proveedor pero NO su lead → el abogado no podía acreditarlo; ahora crea lead `en_initiated_by='counterparty'` con `en_inviter_partner_id`=cliente. (2) la timeline del cliente no incluía los eventos de acreditación del proveedor → ahora sí (OR con partner=provider). Payload de prueba en `tmp_e2e/`.
